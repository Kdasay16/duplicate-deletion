'use strict'

const expect = ('chai').expect

describe('duplicate deletion', function() {
  
  describe('id duplicates', function() {
    it('should delete id duplicates', () => {

    })
    it('should keep the one with the newest date', () => {

    })
    it('should keep the last one in the list if identical dates', () => {

    })
  })

  describe('email duplicates', function() {
    it('should delete email duplicates', () => {

    })
    it('should keep the last one in the list if identical dates', () => {

    })
  })
})
